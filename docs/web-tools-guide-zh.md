# OpenClaw Web Tools 完全指南

[**English**](web-tools-guide.md) | [**简体中文**](web-tools-guide-zh.md)

---

## 目录

- [概述](#概述)
- [快速开始](#快速开始)
- [搜索引擎对比](#搜索引擎对比)
- [Brave Search 配置](#brave-search-配置)
- [Tavily 配置](#tavily-配置)
- [Perplexity Sonar 配置](#perplexity-sonar-配置)
- [web_fetch 网页抓取](#web_fetch-网页抓取)
- [常见问题](#常见问题)
- [最佳实践](#最佳实践)

---

## 概述

OpenClaw 内置了两个轻量级 Web 工具：

| 工具 | 功能 | 适用场景 |
|------|------|----------|
| **web_search** | 网络搜索 | 获取最新信息、资料查找 |
| **web_fetch** | 网页抓取 | 提取网页正文内容 |

> [WARN] **注意**：这些工具**不是**浏览器自动化。对于需要执行 JavaScript 或登录的网站，请使用 Browser 工具。

### 工作原理

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│   你的查询      │────▶│   web_search    │────▶│  搜索引擎 API   │
│  "最新 AI 新闻" │     │   (搜索)        │     │  (Brave/Tavily) │
└─────────────────┘     └─────────────────┘     └─────────────────┘
                                                              │
                                                              ▼
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  可读内容       │◀────│   web_fetch     │◀────│  搜索结果       │
│  (Markdown)     │     │   (抓取)        │     │  (URLs + 摘要)  │
└─────────────────┘     └─────────────────┘     └─────────────────┘
```

**结果缓存**：搜索结果默认缓存 15 分钟（可配置），减少重复请求。

---

## 搜索引擎对比

### 快速决策表

| 特性 | **Brave Search** | **Tavily** | **Perplexity** |
|------|------------------|------------|----------------|
| **免费额度** | 2000次/月 | 1000次/月 | 按量付费 |
| **需信用卡** | [PASS] 是 | [FAIL] 否 | [PASS] 是 |
| **响应速度** | 快 | 更快 | 中等 |
| **结果格式** | 结构化 (标题+链接+摘要) | AI优化结构化 | AI合成答案 |
| **隐私保护** |  最强 |  |  |
| **AI优化** | 传统搜索 | 专为AI设计 | AI原生 |
| **注册难度** | 需绑卡 | 邮箱即用 | 需绑卡 |
| **推荐场景** | 高频使用、隐私敏感 | 轻度使用、快速部署 | 深度研究 |
| **官方支持** | [PASS] 默认支持 | [WARN] 需MCP | [PASS] 原生支持 |

### 选择决策图

```
                    需要网络搜索
                          │
              ┌───────────┴───────────┐
              │   有信用卡吗？        │
              └───────────┬───────────┘
                  │               │
                 否              是
                  │               │
                  ▼               ▼
            ┌─────────┐   ┌──────────────┐
            │ Tavily  │   │ 月需求量？   │
            │  推荐 │   └──────┬───────┘
            └─────────┘          │
                       ┌─────────┴─────────┐
                       │                   │
                    < 1000次            > 1000次
                       │                   │
                       ▼                   ▼
                 ┌─────────┐         ┌────────────┐
                 │ Tavily  │         │ 重视隐私？ │
                 └─────────┘         └─────┬──────┘
                                            │
                                  ┌─────────┴─────────┐
                                  │                   │
                                  是                  否
                                  │                   │
                                  ▼                   ▼
                            ┌──────────┐      ┌──────────────┐
                            │  Brave   │      │ 需要AI合成？ │
                            │       │      └──────┬───────┘
                            └──────────┘              │
                                              ┌─────────┴─────────┐
                                              │                   │
                                              是                  否
                                              │                   │
                                              ▼                   ▼
                                        ┌──────────┐        ┌─────────┐
                                        │Perplexity│        │  Brave  │
                                        │        │        │       │
                                        └──────────┘        └─────────┘
```

### 详细对比

####  Brave Search - 隐私优先的选择

```
[PASS] 优点:
   ✓ 免费额度最高 (2000次/月)
   ✓ 强大的隐私保护，不追踪用户
   ✓ 结构化结果易于解析
   ✓ 官方默认支持，配置简单

[FAIL] 缺点:
   ✗ 必须绑定信用卡（即使免费额度）
   ✗ 传统搜索引擎，无AI增强
   ✗ 注册流程较繁琐
```

**最适合**：
- 每月搜索需求 > 1000 次
- 重视隐私保护
- 有支付能力（信用卡）

####  Tavily - AI 原生选择

```
[PASS] 优点:
   ✓ 无需信用卡，邮箱注册即用
   ✓ 专为 AI Agent 设计
   ✓ 响应速度更快
   ✓ 结果更智能、结构化更好
   ✓ 支持 MCP 协议
   ✓ 提供AI生成的答案摘要

[FAIL] 缺点:
   ✗ 免费额度较低 (1000次/月)
   ✗ 相对较新，生态较小
   ✗ 不是官方默认选项
```

**最适合**：
- 轻度使用 (< 1000次/月)
- 没有信用卡
- 追求快速部署
- 需要AI优化的搜索结果

####  Perplexity Sonar - 深度研究

```
[PASS] 优点:
   ✓ AI合成答案 + 引用来源
   ✓ 实时网络搜索
   ✓ 多步推理能力
   ✓ 答案质量最高

[FAIL] 缺点:
   ✗ 按量付费，无免费层
   ✗ 响应时间较长
   ✗ 使用成本较高
```

**最适合**：
- 需要深度研究和分析
- 重视答案质量而非速度
- 有预算支持

---

## 快速开始

### 步骤 1: 选择搜索引擎

根据你的情况选择：

| 我的情况 | 推荐 |
|----------|------|
|  没有信用卡 | **Tavily** |
|  快速上手试试 | **Tavily** |
|  每月搜索 > 1000次 | **Brave** |
|  重视隐私保护 | **Brave** |
|  需要深度研究 | **Perplexity** |

### 步骤 2: 获取 API 密钥

跳转到对应章节：
- [Brave Search 配置](#brave-search-配置) → 需要 2 分钟
- [Tavily 配置](#tavily-配置) → 需要 1 分钟
- [Perplexity Sonar 配置](#perplexity-sonar-配置) → 需要 3 分钟

### 步骤 3: 配置 OpenClaw

```bash
# 使用配置向导（推荐）
openclaw configure --section web

# 或手动编辑配置
nano ~/.openclaw/openclaw.json
```

---

## Brave Search 配置

### 1. 注册账号

访问 **[Brave Search API](https://brave.com/search/api/)** 注册账号。

> [WARN] **重要提醒**：必须绑定信用卡才能激活免费额度（2000次/月）。

### 2. 获取 API 密钥

登录后进入控制台：

1. 选择 **Data for Search** 计划
   - [WARN] 注意：选择 "Data for Search"，**不是** "Data for AI"
2. 点击 "Generate API Key" 生成密钥
3. 复制保存你的 API 密钥

### 3. 配置 OpenClaw

#### 方法 A: 配置向导（推荐）

```bash
openclaw configure --section web
```

按提示输入 Brave API 密钥即可。

#### 方法 B: 手动配置

编辑配置文件：

```bash
nano ~/.openclaw/openclaw.json
```

添加以下内容：

```json
{
  "tools": {
    "web": {
      "search": {
        "enabled": true,
        "provider": "brave",
        "apiKey": "你的Brave_API_密钥",
        "maxResults": 5,
        "timeoutSeconds": 30,
        "cacheTtlMinutes": 15
      }
    }
  }
}
```

#### 方法 C: 环境变量

编辑 `~/.openclaw/.env`：

```bash
BRAVE_API_KEY=你的Brave_API_密钥
```

### 4. 验证配置

```bash
# 测试搜索功能
openclaw test web-search

# 或在聊天中测试
# 你: 搜索今天的科技新闻
```

### 定价参考

| 计划 | 每月额度 | 价格 |
|------|----------|------|
| **Free** | 2,000 次 | 免费 |
| **Basic** | 15,000 次 | $9/月 |
| **Pro** | 30,000 次 | $25/月 |

---

## Tavily 配置

### 1. 注册账号

访问 **[Tavily](https://tavily.com/)** 注册账号。

> [PASS] **优势**：只需要邮箱，无需信用卡！

### 2. 获取 API 密钥

1. 登录后进入 API Keys 页面
2. 复制你的 API 密钥（格式：`tvly-xxxxxxxxxxxxx`）

### 3. 配置 OpenClaw

Tavily 通过 MCP 协议集成到 OpenClaw。

#### 方法 A: MCP 服务器（推荐）

```bash
# 安装 Tavily MCP 服务器
npm install -g @tavily/mcp-server
# 或使用 Python
pip install tavily-mcp
```

编辑 MCP 配置文件：

```bash
nano ~/.openclaw/mcp.json
```

添加以下内容：

```json
{
  "mcpServers": {
    "tavily": {
      "command": "npx",
      "args": ["-y", "@tavily/mcp-server"],
      "env": {
        "TAVILY_API_KEY": "你的Tavily_API_密钥"
      }
    }
  }
}
```

#### 方法 B: 直接配置（如果支持）

编辑 `~/.openclaw/openclaw.json`：

```json
{
  "tools": {
    "web": {
      "search": {
        "enabled": true,
        "provider": "tavily",
        "apiKey": "你的Tavily_API_密钥",
        "maxResults": 10,
        "searchDepth": "basic",
        "includeAnswer": true,
        "includeRawContent": false
      }
    }
  }
}
```

### 4. 环境变量配置

编辑 `~/.openclaw/.env`：

```bash
TAVILY_API_KEY=你的Tavily_API_密钥
```

### 5. 验证配置

```bash
# 重启 OpenClaw 服务
openclaw restart

# 测试搜索
openclaw test web-search --provider tavily
```

### Tavily API 参数说明

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `apiKey` | string | 必填 | 你的 API 密钥 |
| `query` | string | 必填 | 搜索查询内容 |
| `searchDepth` | string | "basic" | "basic" 基础或 "advanced" 高级 |
| `maxResults` | number | 10 | 返回结果数量 (1-10) |
| `includeAnswer` | boolean | true | 是否包含AI生成的答案 |
| `includeRawContent` | boolean | false | 是否包含原始网页内容 |
| `daysLimit` | number | 3 | 搜索最近N天的内容 |

### 定价参考

| 计划 | 每月额度 | 价格 |
|------|----------|------|
| **Free** | 1,000 次 | 免费 |
| **Growth** | 5,000 次 | $20/月 |
| **Standard** | 20,000 次 | $50/月 |

---

## Perplexity Sonar 配置

### 1. 获取 API 密钥

你有两个选择：

#### 选项 A: 直接使用 Perplexity

1. 注册 Perplexity 账号并获取 API 密钥（以 `pplx-` 开头）

#### 选项 B: 通过 OpenRouter（推荐）

1. 访问 **[OpenRouter](https://openrouter.ai/)**
2. 添加额度（支持加密货币、预付费）
3. 生成 API 密钥（以 `sk-or-v1-` 开头）

> 💡 OpenRouter 的优势：支持加密货币支付，无需信用卡

### 2. 配置 OpenClaw

编辑 `~/.openclaw/openclaw.json`：

**使用 Perplexity 直连**：

```json
{
  "tools": {
    "web": {
      "search": {
        "enabled": true,
        "provider": "perplexity",
        "perplexity": {
          "apiKey": "pplx-你的密钥",
          "baseUrl": "https://api.perplexity.ai",
          "model": "perplexity/sonar-pro"
        }
      }
    }
  }
}
```

**使用 OpenRouter**：

```json
{
  "tools": {
    "web": {
      "search": {
        "enabled": true,
        "provider": "perplexity",
        "perplexity": {
          "apiKey": "sk-or-v1-你的密钥",
          "baseUrl": "https://openrouter.ai/api/v1",
          "model": "perplexity/sonar-pro"
        }
      }
    }
  }
}
```

### 3. 可用模型

| 模型 | 描述 | 推荐场景 |
|------|------|----------|
| `perplexity/sonar` | 快速问答 | 简单事实查询 |
| `perplexity/sonar-pro` | 多步推理（默认） | 复杂问题分析 |
| `perplexity/sonar-reasoning-pro` | 思维链深度分析 | 学术研究、深度分析 |

---

## web_fetch 网页抓取

### 功能说明

`web_fetch` 工具可以：
- 获取网页的 HTML 内容
- 自动提取正文（HTML → Markdown/Text）
- 自动处理重定向
- 缓存结果避免重复请求

### 配置说明

编辑 `~/.openclaw/openclaw.json`：

```json
{
  "tools": {
    "web": {
      "fetch": {
        "enabled": true,
        "maxChars": 50000,
        "maxCharsCap": 50000,
        "timeoutSeconds": 30,
        "cacheTtlMinutes": 15,
        "maxRedirects": 3,
        "userAgent": "Mozilla/5.0 (compatible; OpenClaw/1.0)",
        "readability": true
      }
    }
  }
}
```

### Firecrawl 增强功能（可选）

对于复杂网站（需要JS渲染或有反爬虫保护），可以启用 Firecrawl 作为后备：

```json
{
  "tools": {
    "web": {
      "fetch": {
        "firecrawl": {
          "enabled": true,
          "apiKey": "你的Firecrawl_API_密钥",
          "baseUrl": "https://api.firecrawl.dev",
          "onlyMainContent": true
        }
      }
    }
  }
}
```

### 使用示例

```javascript
// 基础用法
await web_fetch({
  url: "https://example.com/article"
});

// 指定提取模式
await web_fetch({
  url: "https://example.com/article",
  extractMode: "markdown",  // 或 "text"
  maxChars: 10000
});
```

---

## 常见问题

### Q: web_search 返回错误怎么办？

**A**: 请按以下步骤排查：

1. **检查 API 密钥**
   ```bash
   openclaw config show
   ```

2. **检查免费额度**
   - 登录对应服务商的控制台查看剩余额度

3. **测试网络连接**
   ```bash
   # Brave
   curl -I https://api.search.brave.com

   # Tavily
   curl -I https://api.tavily.com
   ```

### Q: 可以同时使用多个搜索引擎吗？

**A**: 可以！配置多个 provider 后按需切换：

```json
{
  "tools": {
    "web": {
      "search": {
        "provider": "brave",  // 默认使用
        "brave": { "apiKey": "BRAVE_KEY" },
        "tavily": { "apiKey": "TAVILY_KEY" }
      }
    }
  }
}
```

### Q: 搜索结果不准确怎么办？

**A**: 尝试以下优化方法：

1. 使用更具体、更精确的查询词
2. 增加 `maxResults` 参数
3. 使用 `freshness` 过滤（仅 Brave）
4. 切换到 Perplexity 获取 AI 合成答案

### Q: web_fetch 无法获取某些网站？

**A**: 常见原因和解决方案：

| 问题 | 解决方案 |
|------|----------|
| 网站需要 JS | 使用 Browser 工具 |
| 有反爬虫保护 | 启用 Firecrawl |
| IP 被封 | 更换 User-Agent 或 IP |
| 内容为空 | 检查网站是否正常访问 |

---

## 最佳实践

### 1. 根据场景选择搜索引擎

```
简单事实查询 ────────▶ Brave / Tavily (快速)
       │
复杂问题分析 ────────▶ Perplexity (深度)
       │
最新新闻资讯 ────────▶ Brave / Tavily (实时)
```

### 2. 合理设置缓存时间

```json
{
  "cacheTtlMinutes": 15  // 根据内容类型调整
}
```

| 内容类型 | 推荐缓存时间 |
|----------|-------------|
| 新闻资讯 | 5-10 分钟 |
| 知识百科 | 30-60 分钟 |
| 实时数据 | 1-5 分钟 |

### 3. 控制使用成本

| 策略 | 说明 |
|------|------|
| 减少 `maxResults` | 降低 API 调用成本 |
| 启用缓存 | 避免重复请求 |
| 监控使用量 | 定期检查 API 配额 |

### 4. 错误处理建议

```javascript
try {
  const results = await web_search({ query: "..." });
  return results;
} catch (error) {
  if (error.message.includes("quota")) {
    return "[FAIL] API 配额已用完，请稍后再试或升级套餐";
  }
  return "[FAIL] 搜索失败，请重试";
}
```

---

## 总结

| 搜索引擎 | 推荐指数 | 最适合人群 |
|----------|----------|------------|
| **Tavily** |  | 新手、无信用卡、快速部署 |
| **Brave** |  | 高频使用、隐私敏感用户 |
| **Perplexity** |  | 深度研究、有预算支持 |

### 新手推荐路径

```
1. 从 Tavily 开始（无需信用卡）
       │
       ▼
2. 熟悉后根据需求升级
       │
       ├──▶ 额度不够？ → Brave（需绑卡）
       │
       └──▶ 需要深度分析？ → Perplexity（付费）
```

---

<div align="center">

**需要更多帮助？**

 [Discord 社区](https://discord.gg/openclaw) |  [GitHub Issues](https://github.com/openclaw/openclaw/issues) |  [官方文档](https://docs.openclaw.ai/)

[ 返回顶部](#openclaw-web-tools-完全指南)

</div>
